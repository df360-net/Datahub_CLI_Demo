"""sales360 - publish the Sales platform's catalog + lineage to DataHub.

Direct token auth (no proxy): the firm enabled PAT-based metadata-service auth, so this emits
straight to GMS with a bearer token. Mirrors the workplace push-only model. See README.md.
"""

# =====================================================================
# datahub_proxy_app1 — .gitignore
# =====================================================================


# ---------------------------------------------------------------------
# Secrets — NEVER commit these
# ---------------------------------------------------------------------
.env
.env.local
.env.*.local
*.pem
*.key
*.pfx

# Internal agent-instruction file — references private hosts/paths; not for publication
CLAUDE.md
secrets/


# ---------------------------------------------------------------------
# Daily data files — generated by tools/generate_daily_file.py
# Keep the inbound/ folder but ignore its contents.
# ---------------------------------------------------------------------
data/inbound/*
!data/inbound/.gitkeep
data/outbound/
data/tmp/


# ---------------------------------------------------------------------
# Logs
# ---------------------------------------------------------------------
*.log
logs/


# ---------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Build / dist
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/

# Installer / packaging
MANIFEST

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# Virtual environments
venv/
.venv/
env/
ENV/
env.bak/
venv.bak/


# ---------------------------------------------------------------------
# IDE / editor
# ---------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.project
.pydevproject


# ---------------------------------------------------------------------
# OS noise
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini


# ---------------------------------------------------------------------
# Local SQL scratch
# (db/*.sql in this repo is intended for sa to run via sqlcmd;
#  local-test variants should be gitignored)
# ---------------------------------------------------------------------
db/*.local.sql
db/scratch/
